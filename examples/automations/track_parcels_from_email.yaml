# Extract Paack tracking codes from incoming e-mail and register them
# with `paack.track_parcel`, so parcels start tracking the moment the
# shipping confirmation lands in your inbox — no manual entry.
#
# Requirements:
#   - one or more core IMAP config entries with "text" enabled under
#     "message data" and the max message size raised (carrier mails are
#     long) — full setup guide and pitfalls: track_parcels_from_email.md
#   - optional: an `ai_task` entity for the AI fallback branch; delete
#     the whole `else:` block if you don't use one
#   - an `input_text.paack_delivery_postcode` helper containing the postcode
#     of the Paack hub this automation should use
#   - PRIVACY: by default this fires on EVERY incoming mail and (with the
#     AI branch) can send mail bodies to your AI Task. Scope the IMAP
#     search to the carrier's sender first — see "Scope & privacy" in the .md.
#
# The regex is deliberately written without backslashes (`(?<![0-9A-Za-z])`
# instead of `\b`, `[0-9]` instead of `\d`): Jinja treats `\b` inside a
# string literal as a backspace escape, so the pattern would silently
# never match.

alias: "Paack: track from e-mail"
description: >-
  Listens to imap_content events from all IMAP entries. Fast regex path for
  Paack tracking codes; optional AI fallback for mails whose layout the
  regex does not recognise.
mode: queued
max: 10

triggers:
  - trigger: event
    event_type: imap_content

variables:
  sender: "{{ trigger.event.data.sender | default('') | lower }}"
  subject: "{{ trigger.event.data.subject | default('') }}"
  body: "{{ trigger.event.data.text | default('') }}"
  content: "{{ subject ~ ' ' ~ body }}"
  paack_postal_code: "{{ states('input_text.paack_delivery_postcode') | trim }}"
  # Paack codes have no stable public prefix. Limit this example to a known
  # retailer sender and adapt the pattern to that retailer's mail format.
  paack_codes: >-
    {{ content | regex_findall('(?<![0-9A-Za-z])(?:EXAMPLE|EXA)[0-9A-Za-z]{6,}')
       | unique | list }}
  # Keyword gate for the AI fallback — localise to your language(s).
  is_parcel_mail: >-
    {{ content | lower is search('package|shipped|out for delivery|track|shipment|delivery') }}

conditions:
  # initial=true means "new message"; false means a duplicate trigger of
  # the SAME message (e.g. after a reconnect) — requiring it is free dedup.
  - condition: template
    value_template: "{{ trigger.event.data.initial | default(true) }}"
  - condition: template
    value_template: "{{ paack_postal_code != '' }}"
  - condition: template
    value_template: "{{ is_parcel_mail or paack_codes | count > 0 }}"

actions:
  - repeat:
      for_each: "{{ paack_codes }}"
      sequence:
        - action: paack.track_parcel
          data:
            tracking_code: "{{ repeat.item }}"
            postal_code: "{{ paack_postal_code }}"
  - if:
      - condition: template
        value_template: "{{ paack_codes | count > 0 }}"
    then:
      - action: notify.mobile_app_your_phone
        data:
          title: "📦 Paack parcel picked up from mail"
          message: >-
            {{ paack_codes | join(', ') }} (mail from {{ sender }})
    else:
      # ---- Optional AI fallback — delete this block if you have no ai_task entity ----
      - action: ai_task.generate_data
        data:
          task_name: paack-tracking-code-extraction
          entity_id: ai_task.your_ai_task_entity
          instructions: >-
            Below is an e-mail. Determine whether it contains a Paack
            parcel tracking code. Paack codes are
            6-30 uppercase letters and digits, usually starting with a
            prefix such as EXAMPLE or EXA. Only return a real tracking
            trace code, never an order, customer or invoice number. If the
            mail is from a different carrier, answer 'other'. When in
            doubt: 'none'.

            Sender: {{ sender }}
            Subject: {{ subject }}
            Body:
            {{ body[:6000] }}
          structure:
            carrier:
              description: Whether a Paack tracking code was found
              required: true
              selector:
                select:
                  options: ["paack", "other", "none"]
            tracking_code:
              description: The tracking code, or empty if none
              required: false
              selector:
                text:
        response_variable: ai_result
      - choose:
          - conditions: "{{ ai_result.data.carrier == 'paack' and (ai_result.data.tracking_code | default('') | trim) != '' }}"
            sequence:
              - action: paack.track_parcel
                data:
                  tracking_code: "{{ ai_result.data.tracking_code | trim }}"
                  postal_code: "{{ paack_postal_code }}"
              - action: notify.mobile_app_your_phone
                data:
                  title: "📦 Paack parcel picked up from mail (AI)"
                  message: "{{ ai_result.data.tracking_code | trim }} (mail from {{ sender }})"
          - conditions: "{{ ai_result.data.carrier == 'other' and (ai_result.data.tracking_code | default('') | trim) != '' }}"
            sequence:
              - action: notify.mobile_app_your_phone
                data:
                  title: "📦 Tracking number found – not a Paack parcel"
                  message: "{{ ai_result.data.tracking_code | trim }} (mail from {{ sender }}, subject: {{ subject }})"
